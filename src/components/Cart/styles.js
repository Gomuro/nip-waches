import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  toolbar: { marginTop: "100px", justifyContent: "space-between" },
  title: {},
  itemsContainer: {
    justifyContent: "space-aro",
  },
  cardDetails: {
    display: "flex",
    marginTop: "10%",
    width: "100%",
    justifyContent: "space-between",
  },
}));
